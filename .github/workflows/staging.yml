name: Deployment to staging

on:
  push:
    branshes:
      - main

jobs:
  test:
    runs-on: ubunto-latest
    steps:
      - name: checkout code
        uses: actions/checkout@v6

      - name: install dep
        run: npm i

      - name: run test
        run: npm run test
